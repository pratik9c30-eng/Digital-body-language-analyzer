export function ExplainabilityPanel({reasons,bot}:{reasons:{reason:string;z_score:number}[];bot:number}){return <div className="panel explain"><div className="panel-title"><span>Behavioral anomaly explanation</span><small>MODEL TELEMETRY</small></div>{reasons.length?reasons.map((r,i)=><div className="reason" key={i}><i style={{width:`${Math.min(100,Math.abs(r.z_score)*25)}%`}}/><span>{r.reason}</span><b>{r.z_score>0?'+':''}{r.z_score}</b></div>):<div className="quiet">No meaningful behavioral anomaly detected.</div>}<div className="bot-row"><span>Automation likelihood</span><strong>{Math.round(bot*100)}%</strong></div></div>}
