import { Area, AreaChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
export function DriftTimeline({score}:{score:number}){const data=Array.from({length:12},(_,i)=>({t:i,trust:Math.max(40,score+(i-7)*1.7+Math.sin(i)*3)}));return <div className="panel chart-panel"><div className="panel-title"><span>Trust drift</span><small>LAST 18 MIN</small></div><ResponsiveContainer width="100%" height={155}><AreaChart data={data}><defs><linearGradient id="trust" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#f4b860" stopOpacity={.35}/><stop offset="1" stopColor="#f4b860" stopOpacity={0}/></linearGradient></defs><XAxis dataKey="t" hide/><YAxis domain={[0,100]} hide/><Tooltip contentStyle={{background:'#202c31',border:'1px solid #415158'}}/><Area type="monotone" dataKey="trust" stroke="#f4b860" fill="url(#trust)" strokeWidth={2}/></AreaChart></ResponsiveContainer></div>}
