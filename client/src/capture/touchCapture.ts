export class TouchCapture { private pressure:number[]=[]; private radius:number[]=[]; onPointer(e:PointerEvent){if(e.pointerType==='touch'){this.pressure.push(e.pressure || .5);this.radius.push((e.width+e.height)/2);}} snapshot(){const avg=(x:number[])=>x.length?x.reduce((a,b)=>a+b,0)/x.length:.5;return {touch_pressure:avg(this.pressure),touch_radius:Math.min(1,avg(this.radius)/40)};} }
